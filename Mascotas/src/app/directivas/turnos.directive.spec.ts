import { TurnosDirective } from './turnos.directive';

describe('TurnosDirective', () => {
  it('should create an instance', () => {
    const directive = new TurnosDirective();
    expect(directive).toBeTruthy();
  });
});
